[project]
name = "renpy-translator"
version = "0.4.0"
description = "Traslate you Ren'Py project using a LLM"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "openai>=2.37.0",
]

[tool.ruff.format]
quote-style = "single"
